{
  "name": "FocusChain Flutter",
  "image": "ghcr.io/cirruslabs/flutter:stable",
  "forwardPorts": [3000],
  "portsAttributes": {
    "3000": {
      "label": "Mr. Rex Flutter Web",
      "onAutoForward": "openBrowser"
    }
  },
  "postCreateCommand": "apt-get update && apt-get install -y git-lfs && git lfs install && flutter config --enable-web && flutter pub get",
  "customizations": {
    "vscode": {
      "extensions": [
        "Dart-Code.dart-code",
        "Dart-Code.flutter"
      ]
    }
  }
}
